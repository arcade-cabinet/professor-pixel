import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.arcadecabinet.professorpixel',
  appName: "Pixel's PyGame Palace",
  webDir: 'dist',
};

export default config;
